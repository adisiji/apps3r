<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ToolTipLayoutRedStyle" parent="ToolTipLayoutDefaultStyle">
        <item name="android:textAppearance">@style/ToolTipTextStyle</item>
    </style>

    <style name="ToolTipLayoutGreenStyle" parent="ToolTipLayoutDefaultStyle">
        <item name="ttlm_backgroundColor">@color/green_button</item>
        <item name="android:textAppearance">@style/ToolTipTextStyle</item>
    </style>

    <style name="ToolTipTextStyle">
        <item name="android:textColor">@color/white_200</item>
    </style>

    <style name="MySwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/green_button</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/grey_focus</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/green_button</item>
    </style>



</resources>
